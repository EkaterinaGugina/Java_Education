public class lib {
     
     
    static void printArray(int arr[])
    /**
    * Вывод на экран массива
     */
    {
        int n = arr.length;
        for (int i = 0; i < n; i++)
            System.out.print(arr[i]+" ");
        System.out.println();
    }   
}
